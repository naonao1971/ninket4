# にんケット４ ランディングページ

作成したファイル:

- index.html
- styles.css

プレビュー方法（簡易）:

```bash
# ローカルで簡単に確認する
python3 -m http.server 8000
# ブラウザで http://localhost:8000/ を開く
```

ファイルを編集したい場合は `index.html` と `styles.css` を直接編集してください。
